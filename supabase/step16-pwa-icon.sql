-- ============================================================
-- ステップ16 PWAアイコンを設定画面からアップロード可能に
-- Supabase ダッシュボード → SQL Editor に貼り付けて実行してください。
-- 冪等（再実行しても安全）。
--
-- stores.pwa_icon_url に、管理画面(/admin)をホーム画面に追加した際の
-- アイコン画像URLを持たせる。アップロード自体は既存の photos バケット
-- （header/footer写真と同じ、公開読み取り・書込はスタッフのみ）を再利用する
-- ため、Storage側のポリシー追加は不要（supabase/step6-storage.sql が既に対応済み）。
-- ============================================================

alter table stores add column if not exists pwa_icon_url text;

-- ============================================================
-- 確認用（実行後）:
--   select pwa_icon_url from stores limit 1;  -- null でOK（設定画面から追加する）
-- ============================================================
